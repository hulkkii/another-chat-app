# Codex Instructions

- This repo only contains documentation for now.
- After making changes run `echo "No tests"` to simulate tests.
- Keep docs organized under `docs/` and update README if needed.
